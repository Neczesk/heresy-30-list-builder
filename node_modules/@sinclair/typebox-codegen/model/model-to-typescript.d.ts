import { TypeBoxModel } from './model';
export declare namespace ModelToTypeScript {
    function GenerateType(model: TypeBoxModel, $id: string): string;
    function Generate(model: TypeBoxModel): string;
}
